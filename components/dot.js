export default class Dot {
  constructor () {
      this.posx = Math.random()*500; // position X
      this.posy = Math.random()*500; // position Y
      this.direction = Math.random()*360; // direction en degré
  };
  
}